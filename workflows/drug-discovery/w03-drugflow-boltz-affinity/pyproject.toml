[project]
name = "w03-drugflow-boltz-affinity"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "horus-environments>=0.1.2",
  "horus-runtime>=0.3.0",
  # generate runs in a Singularity container submitted through Slurm.
  "horus-singularity>=0.0.1",
  "horus-slurm>=0.0.1",
]
