[project]
name = "w10-amber-protein-ligand-complex-md-setup"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "horus-docker",
  "horus-environments>=0.1.2",
  "horus-runtime>=0.1.6",
]
