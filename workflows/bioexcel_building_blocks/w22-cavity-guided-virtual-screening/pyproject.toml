[project]
name = "w22-cavity-guided-virtual-screening"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "horus-docker",
  "horus-environments>=0.1.2",
  # 0.2.2 is the floor: earlier versions report a map's gather input as an
  # un-promoted root artifact, which fails the repo's sanitize-check CI gate.
  "horus-runtime>=0.2.2",
]
